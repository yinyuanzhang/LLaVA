目标：
1. 生成两个数据集main result & cache复用率[每张图片原始background : 原始object]
    - 存储 fuss索引全部转移至 ～/model下
    - 比较jinxin2 与 nanhu现有代码差异，提交至github
    - 生成检索匹配成功率代码 (方便后续通过 resnet、vit等方式)
    - 观察代码更改细节，一键参数化配置代码 [争取21:30 前完成]
    - 替换成新的数据集，测试结果
    - 调研textvqa的数据(测试集的数目)

2. 最好是不更改代码，通过config参数控制上述生成



换个思路：
适配代码无误后，我们再进行代码生成



模型 [先默认用 window-16 模型进行使用]
数据 
系统 




图像生成：
1. 如何并行生成 [一会解决下]
2. 可以改为只在一个模型进行生成
2. 如何利用beiyou服务器(次要，目前不是那么紧迫)



VQA检测结果：
我为什么现在状态不好？
- 杂/乱
明天的目的就只有解决下面这些，不包含解决ui agent数据集
杂/乱在哪里？
1. 原生 vs. vit独立编码
- 测试是否支持，若支持，则记录下。

2. nanhu vs. jinxin2
- 代码未统一
- 统一后上传至github

3. 统一平台的数据集与自定义数据集
- list列出来
- 自定义数据集测试原生的结果

4. 使用cache库的代码和原代码糅合在一起  [明早完成]
- 抽离
- 使用变量控制 write cache vs selective from cache
- 检索命中率 / 以及为什么总是命中12(甚至匹配程度100%)
- 若是一对一匹配命中的话，其result
- 生成每张图片的背景 vs 目标概率统计图，并给出平均复用情况(假设不考虑缓存占用问题，平均每张图片复用了多少背景)。

5. 对多种数据集的支持情况
- 每种数据集的区别在哪里？
- 在vqa_eval调用处，能够实现random挑选数据集，并将挑选的list和bagel生成进行联动。
(明早换个数据集试一下)


6. 更全面的评估
- 完全相同 / python制造的样本 / bagel生成的图片样本 

7. 目标切割方式
- 切割方式：按照块进行切割，太过考虑目标的话很可能使得背景占比过小，影响效率。
- 方式1: 增大块确定的方式，比如 从 4*4 到 6*6 到 8*8 到 12*12


cache_load_way 
- none
- write-only (预先加载)
- read-only
- auto (符合相似度就load，不符合就计算。 )


F1:0.855
F1:0.854(0.01)
F1:0.852(0.1 全命中)




背景token占比：47.58%
缓存命中率：84.86%
F1:0.846





需要明确的数据信息。



先不要考虑ui agent数据集的事情，根据 qwen2.5vl制作。


参数引入：
1. paration_way
2. max / avg-pool

















修正后的代码只支持batch_size为1，不支持训练模式


不同数据集存储于不同位置 (还是统一放在参数指令处处理，方便处理)


5点钟之前验证 none、read-only的结果、完整的统计指标
6点钟之前验证 其他数据集的结果



理论来说，数据集的筛选应该在调用外面处理



就先按照目标的背景 & 目标 token数目处理吧，之后再考虑怎么优化。



23点前完成

从14点开始，将近8小时
14-18 4小时，完成主体工作 (模型拆分 + 新的数据集)

19-23 4小时，更精细的数据分析




6.20 - 6.30 共10天 (冲刺10天 deadline)
- llava_1.5v_7b 模型在3个数据集上的accuracy效果
- qwen 模型在上述3个数据集上的accuracy效果 [1天部署跑通、2天微调、2天适配数据集]
- llava_1.5v_7b 模型端到端的微调方式 + 其他可考虑的方式
- object-only、ragcache、ragcache-fuzzy 的比较结果 + 兼容适配











AndroidControl 数据集准备
- 15:00 环境安装好 done
    conda create -n android_control python=3.10
    pip install tensorflow pandas pillow
    pip install .

- 16:00 数据集准备好
    - 输入是什么
    - 输出是什么
    - 如何评估、是不是还得抽取数据的tree


- 17:00 完成数据集过滤
- 18:00 完成 使用 Qwen2.5vl 数据集评估







明天只有一个任务：就是实现 llava对模型和数据集的适配

第一步：先实现 object-only、fuzzy-cache
先测试原本的 native、segement-cache 是否能正常运行
统一更改为 method_type 进行调配； cache-load-way 不发生改变，支持 "read-only"、"write-only"、"read-load" 三种方式



git为什么不行？




目前能测出 native、能测出 segement-cache



第二步：再实现数据集的适配

