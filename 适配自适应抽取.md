1. 动态阈值 30min done
2. 自适应抽取特征 2h
    - 动态参数指定 done
    - 缓存库的初始化(路径、维度) done
    - vit级别的mask映射回 特征抽取[最后查看]  done(还需要重新过一下 vit内部逻辑)) done
    - 缓存库的 key-value设计 done
    - 指标监控 done
3. launch.json 阈值 30min
4. 通读现有的 llava实现 [关键点1:llava与qwen25vl的目标检测的位置为什么不同？ 关键点2:现有的vit的内部流程]


undo:
dynamic_cache_path 改为和Qwen25vl相同的格式，整体的检索库初始化应该和Qwen25vl保持一致 done
新引入了无用的 bg_position_ids 和 image_position_ids
self.get_model().stats_collector 去掉 read-only 和 read-load 模式的限制  done
是否重复执行了 获取 background_mask
小结：主要是raw_image_mask是否准确、position_ids是否准确、



1. 阅读完整代码
2. 阅读更改后的代码
3. debug更改后的代码
4. 运行结果 done
---------------
5. 扩展至 video数据集
6. 重新理清并实现 cacheblend



明天上午：
- 现有实现逻辑捋一遍(核心就是 把原始图像传过去，把mask传过去，且二者能对齐) 10点前



- cacheblend现有实现完成 14点前 

明天下午：
- 扩展新的数据集
- 整个项目搬迁
- 修复位置编码问题






明天主要的任务是跑结果、确认cacheblend实现正确、调参(主要是特征提取的方式)、调参(主要是video截取的方式)、llava实现的position_ids 位置对齐

后天的任务是time-cost、论文写作 [尽量完成基本实验的情况下 进行论文写作]
- 手机跑一下llava的测试时间 (7B量化后的模型)





llava理解：【极其关键！！】
- 参数如何传递？
    load_pretrained_model(model_args = args)  # 🎯 传递整个args对象
    model = LlavaLlamaForCausalLM.from_pretrained(model_args,  # 🎯 传递 model_args (就是args)
    class LlavaLlamaForCausalLM(LlamaForCausalLM, LlavaMetaForCausalLM): 通过 config.dataset = model_args[0].dataset，将 model_args参数传递至 config中
    LlavaMetaModel 通过 self.use_lightweight_query_key = getattr(config, 'use_lightweight_query_key', False) 从 config中提取参数

- 缓存系统如何构建
    根据method_type 分别构建不同的缓存系统，同时统一构建统计指标

- 模型精度
    这里涉及好几个模型，容易出现精度问题。 比如，主模型以 bfloat16运行，其他模型参数也被强制转换(为什么会出现模型被强制转换的问题？)
    原始的llava模型明确以 float16精度
    这里把 query_key_extractor的初始化放到 LlavaMetaModel的init下，相当于整个llava模型初始化的位置
        -- 方案1：在转换后重新设置精度,在 builder.py:223 后添加(还是不行，主要涉及到 float32-float16-float32,溢出)
        -- 方案2：重写 visual_tower 的.to 方法(内部实现精度强转)
        -- 方案3: 加载完成后全部重新加载
        -- 最终是 通过修改 builder.py 完成子模型的强转

- write-only 模式下实现
    llava_llama.py 中self.prepare_inputs_labels_for_multimodal 内部实现了 1. 完整的forward 后kv_cache 存储写入(默认准备好大量的数据) 2. 调用 return super().generate 不影响后续pipeline(不传入参数或者传入默认参数就相当于未使用cacheblend核心逻辑)
    llm需要的 inputs_embeds、position_ids、attention_mask

- read-load 模式下实现
    元数据准备(第一遍forward获取input_embedding、基于原图像检索、重建input_embedding、获取元数据)
    llm计算(回退判断)



- llava 与 Qwen25vl 关于cacheblend实现最大不同
    Qwen25vl的input_embedding 的图片顺序是交叉的，而llava的input_embedding是[fg,bg]。  所以，影响到后续的 _extract_vision_features 和 _rebuild_inputs_for_variable_cache。


- prepare_inputs_labels_for_multimodal 内部对 input_ids 是否有处理， input_ids 和 input_embedding 是否本身就不对齐










- 晚上可以调用，查看测试结果


llava中值得 Qwen25vl学习的地方：
- 边界条件：背景tokne为0，不缓存
- 



关于cacheblend的实现，我建议自己手动进行更改。

cacheblend
write-only:
缓存写入是否准确、完整

read-load:
元数据的维护 [元数据的内容以及生命周期的管理，建议和qwen25vl保持一致]
background与前景开关保留
第0层完整计算、第1层计算差异、后面层进行缓存resue



三小时冲刺:
- 目前llava的cacheblend实现方式
- Qwen25vl的cacheblend实现方式
- write-only 模式下能写入成功


- kv cache 缓存 CacheBlendKVController 初始化成功
- write-only模式下 需要forward完整的pipeline，然后从 forward中取出 kv_cache
    write-only模式下注意所有的元数据维护



请仔细阅读 /data/zyy/Qwen2.5-VL 目录下 cacheblend_qwen_generation.py 中cacheblend的实现。 该实现是完整且经过验证后是准确的。请分析其是如何实现，比较现有llava与 qwen25vl关于cacheblend的实现。

好的，目前我们llava项目关于 cacheblend 的实现还存在很多问题。以Qwen2.5vl 的cacheblend的实现为标杆的话，我们逐一优化修改。  首先是 元数据准备 和  特征提取阶段、缓存搜索阶段。 
Qwen2.5vl的元数据很全面，而且完全支持后续重计算和指标的计算；特征提取支持轻量级的特征提取，这里我们在llava_arch.py 也已经有一些特征提取的实现。 。请优化这三部分，优化完成后我们再优化 后续的输入重建和kv对齐。





实验内容记录：
llava_1.5v_7b 运行 pope数据集 result (1. 统计指标有问题 2. 命中率也和Qwen系列有较大差距)





3小时 llava_cacheblend 专心实现 [心态比较急切，还是得稳住给出准确实现]




llava_native: 
    - 统计指标有问题





问题1: prepare_inputs_labels_for_multimodal 是否涉及对 input_ids 等的更改，目前是直接跳过



20-22点: 
过一遍 llava_llama.py 和 llava_arch.py、cacheblend_llama.py，分析是否存在问题。




遗留点：
- 位置编码问题 
- 先确保元数据管理正确



1. images并非真正的原始图像
2. final_is_foreground_mask 与 image_embeddings 顺序不对应 (需要改造下)  或者说本身进行检索的和 image_embeddings没说一定要保持一致
    确实，Qwen25vl系列返回的是原始顺序，这里llava返回的是 背景-前景拼接后的顺序 (实在不行，只能继续更改vit内部，返回原始顺序的 image_embeddings)   done
3. if not self.use_fg_cache:  这个配置的设置在哪里  done
4. cacheblend 未初始化轻量级 query




2-3点 cacheblend 元数据处理
    images -> raw_images 处理  done


3-5点 cacheblend 核心计算处理
5-6点 排查问题，解决







优化选项：
- 位置编码问题
- 全是背景怎么半？
- 全是前景怎么半？
- 背景/前景未命中时，是否回退为原本的实现？



使用正确的位置编码：




今晚

先解决 pope数据集上的 llava & llava_cacheblend


争取 22:00 前完成 cacheblend 的实现
- 基本标准：out输出是正确的 yes/no等

22-23 点 查看llava是如何处理位置编码的，同时尽可能修复




包装器 & 继承 到底该在什么时候使用？
需要学习的：(如何在不改变权重的情况下进行修改)

  Qwen2.5-VL 的方法（继承模式）

  class CacheBlendQwen2_5_VLAttention(CacheBlendAttentionMixin, Qwen2_5_VLAttention):
      def forward(self, ...):
          # 直接使用继承的权重
          query_states = self.q_proj(hidden_states)  # 权重路径：q_proj
          key_states = self.k_proj(hidden_states)    # 权重路径：k_proj
          # ... CacheBlend 逻辑

  我们 LLaVA 的方法（包装器模式）

  class CacheBlendLlamaAttention(nn.Module):
      def __init__(self, attn: LlamaAttention, ...):
          self.attn = attn  # 包装器
          # 权重实际在：self.attn.q_proj (需要映射！)

  💡 Qwen2.5-VL 解决方案的精髓

  1. 在 DecoderLayer 的 __init__ 中替换

  class CacheBlendQwen2_5_VLDecoderLayer(Qwen2_5_VLDecoderLayer):
      def __init__(self, config, layer_idx):
          super().__init__(config, layer_idx)  # 先创建原生模块(包括权重)
          # 关键：替换 attention，但保持权重路径一致
          self.self_attn = CacheBlendQwen2_5_VLAttention(config, layer_idx)

  2. 权重路径保持不变

  - 预训练权重：layers.0.self_attn.q_proj.weight
  - CacheBlend权重：layers.0.self_attn.q_proj.weight ✅
  - 完全匹配，无需映射！




这是一个非常深刻的洞察，您一针见血地指出了为什么 Qwen2.5-VL 的实现方式更优雅，以及为什么 LLaVA 的实现（在您上一版代码中）被迫使用 enable_cacheblend_attention 这种“热替换”+“手动权重复制”的方案。

1. Qwen2.5-VL (继承) 方案：
路径：CacheBlend...DecoderLayer 继承 Qwen...DecoderLayer。

路径：CacheBlend...Attention 继承 Qwen...Attention。

关键：CacheBlend...Attention 通过 super().__init__ 调用了 Qwen...Attention 的构造函数，因此 self.q_proj, self.k_proj 等模块被直接创建为它自己的成员。

结果：当 from_pretrained 加载权重时，它要找的路径是 layers.0.self_attn.q_proj.weight。在您的新模型中，这个路径完全不变 (self.self_attn 是 CacheBlend...Attention 实例，它直接拥有 q_proj)。

评价：完美。 权重路径 100% 匹配，无需任何额外映射。

2. LLaVA (包装器) 方案 (您上一版代码)：
路径：CacheBlend...DecoderLayer 继承 LlamaDecoderLayer。

路径：CacheBlend...Attention 不继承 LlamaAttention，而是包装它（self.attn = attn）。

关键：LlamaAttention 实例被“藏”在了 self.attn 属性的内部。

结果：当 from_pretrained 加载权重时，它要找的路径是 layers.0.self_attn.q_proj.weight。但在您的新模型中，这条路径不存在。真正的路径变成了 layers.0.self_attn.attn.q_proj.weight。

评价：路径不匹配。 这导致 from_pretrained 无法自动加载权重。

结论
您对这两种模式的分析完全正确。

这也完美地解释了您在 llava_llama.py 中（上一版代码）设计 enable_cacheblend_attention() 方法的根本原因：

您使用“包装器”模式（self.attn = attn）导致了权重路径不匹配，因此您必须：

先让 LLaVA 用原生架构（LlavaLlamaModel）加载所有权重。

加载完成后，再调用 enable_cacheblend_attention() 来执行“热替换”。

在“热替换”时，手动将权重从 original_layer.self_attn.q_proj 复制到 cacheblend_layer.self_attn.attn.q_proj。

您的讲述（“包装” vs “继承”如何影响权重路径）是完全正确的，并且是理解这两种实现方式差异的核心。






问题0:
# ============== 步骤2：计算KV序列长度 (与原生相同) ==============
kv_seq_len = key_states.shape[-2]
if past_key_value is not None:
    kv_seq_len += past_key_value[0].shape[-2]

这里存在调用问题 done


问题一：
请你仔细分析 关于segementation_cache 实现过程中的位置编码问题。 即：

问题二：
请分析 sparse_attention, qwen25vl是sparse output(残差处做了处理)，这里是full_output(不重要token置于0)



1. sparse_attention 如何解决  (sparser_output 和 残差的处理)
2. past_key_value 的处理


目标：cacheblend有正常输出即可。 重点还是要放在 llava本身的实现上。

明晚尝试迁移下 任务  把llava从nanhu迁移到beiyou



15:00 确认llava的修改仍需要 通过继承的方式实现 sparse attention

16:00 修改完毕

17:00 同时确认，调试完毕 [基本要求: output为 yes/no等]







分步调试：

元数据准备阶段：
- 先直接debug，查看kwargs的具体内容
- 第一标准：kwargs 元数据格式正确、old_kvs 数据格式正确、input_embeddings、position_ids 格式正确

cacheblend核心计算环节：
- attention_mask 目前是2D，而llava计算需要4D (看下 Qwen25vl是否需要)
- 原本实现为什么没问题，应该是deocde或者哪里进行了修改，将attention_mask 从2D转向4D

- 第0层全计算
- 第1层计算 imp_indices，imp_indices是否正确？
- 



- cacheblend_metadata 状态管理问题，从头过一遍 15:00 前






参数管理：
CacheBlendLlamaDecoderLayer
- cacheblend_metadata = kwargs.get("cacheblend_metadata", {})
    相当于完全依赖于 kwargs中的cacheblend_metadata
    - cacheblend_metadata.get("check_layers")
    - cacheblend_metadata.get("imp_indices") 相当于把参数从 cacheblend_metadata 转化到 


除了略显复杂外，基本正确。


CacheBlendLlamaAttention
- 依据参数进行计算，除了更新下 imp_indices 
- 



元数据只需要更新的三个地方：
- 计算 imp_indecies
- 根据 imp_indecies 更新 cacheblend_status
- 每个样本都需要进行新建



应该是 从 prefill阶段结束后 传至decode阶段 出的错。




# 临时设置正确的 parent_ctx 是否可以删除





liner attention: 




项目书
llava_cachebkend




原生 llava 的output

llava_cacheblend 的 output


可以统一把 2D mask 改为 4D mask




从头开始处理
- 元数据准备好 debug能看出来
- attention_mask 在哪里处理比较合适
- position_ids 怎么初步定义比较合适
- 一堆冗余逻辑怎么处理
- cacheblend内部实现直接用的 qwen25vl的，应该没问题
- 元数据管理: prefill和decode的切换、样本与样本之间的切换[需要解决]
- 目前问题还是 输出 ''




元数据管理：
[一次性]
- LlavaLlamaForCausalLM 中 init self.cache_fuse_metadata [check collect imp_indices]
- LlavaLlamaForCausalLM 中 init self.old_kvs

[new sample] [关键1: 样本与样本之间信息的传递通过此] [关键2: cache_fuse_metadata中的其他元素都必须要清空] [关键3: self.old_kvs 放在外面]
- LlavaLlamaForCausalLM 中 forward 
    _update_cacheblend_state  包括 cache_fuse_metadata 和 old_kvs

[_handle_cacheblend_read_load]
- self.cache_fuse_metadata["old_kvs"] = old_kvs (old_kvs 到底是在 cache_fuse_metadata内部还是外面)

[唯一一次 LlavaLlamaForCausalLM 与 CacheBlendLlavaLlamaModel 参数共享]
    self.model.cache_fuse_metadata = self.cache_fuse_metadata
    self.model.old_kvs = self.old_kvs

------------------------------------------------------------------------------

- CacheBlendLlavaLlamaModel 每个新样本均会 forward，每次forward调用 _prepare_cacheblend_kwargs
    新组织 cacheblend_kwargs [cacheblend_status、 cacheblend_metadata、 old_kvs]，cacheblend_kwargs被放置在 kwargs中传播。
    此此保证每个样本的元数据都是全新的，包括 cacheblend_status 也是0


------------------------------------------------------------------------------

- CacheBlendLlamaModel

无操作
除了 会更新一次位置编码 cacheblend_metadata["_position_updated"]，[关键：但是感觉没有真正更新到 kwargs参数中]
我理解 CacheBlendLlamaModel 会发起调用 decoder_layer，其他没干啥

------------------------------------------------------------------------------

- CacheBlendLlamaDecoderLayer
    根据 is_check_layer 和 has_imp_indices 决定 cacheblend_status
    把 cacheblend_status 和 cacheblend_old_kvs 抽取一层传递给 cacheblend_old_kv

关键：处理残差连接

------------------------------------------------------------------------------

- CacheBlendLlamaAttention
    - updata important_indeices


------------------------------------------------------------------------------


如何修复问题：
还是得从 外面代码逐层往内部代码进行修复。





原生实现：
position_ids、attention_mask 均为none


  LlamaForCausalLM 本身确实没有定义 generate 方法！ 这个方法是通过继承链获得的：

  1. 继承关系:
  LlavaLlamaForCausalLM → LlamaForCausalLM → LlamaPreTrainedModel → PreTrainedModel
  2. generate 方法的来源:
    - generate 方法定义在 /transformers/generation/utils.py 中
    - 它是 GenerationMixin 类的方法
    - PreTrainedModel 继承了 GenerationMixin
    - 所以所有继承自 PreTrainedModel 的模型都自动获得了 generate 方法

  函数调用链分析

  完整的调用流程：

  1. llava_llama.py:432-437 - 你的代码调用:
  return super().generate(
      position_ids=position_ids,        # 此时为 None
      attention_mask=attention_mask,    # 此时为 None  
      inputs_embeds=inputs_embeds,
      **kwargs
  )
  2. generation/utils.py - GenerationMixin.generate():
    - 这是真正的 generate 方法实现
    - 在生成循环中，会反复调用 prepare_inputs_for_generation 和模型的 forward 方法
  3. modeling_llama.py - LlamaForCausalLM.prepare_inputs_for_generation():
  def prepare_inputs_for_generation(self, input_ids, past_key_values=None, attention_mask=None, inputs_embeds=None, **kwargs):
      # 关键代码：在这里生成 position_ids 和 attention_mask
      if attention_mask is not None and position_ids is None:
          # create position_ids on the fly for batch generation
          position_ids = attention_mask.long().cumsum(-1) - 1
          position_ids.masked_fill_(attention_mask == 0, 1)

      return {
          "position_ids": position_ids,      # 这里不再是 None！
          "attention_mask": attention_mask,  # 这里也不再是 None！
          "inputs_embeds": inputs_embeds,
          # ...
      }
  4. 最终调用 forward:
    - generation/utils.py 中调用 outputs = self(**model_inputs)
    - 这会调用到你的模型的 forward 方法 (llava_llama.py:330)
    - 此时 position_ids 和 attention_mask 已经不是 None 了！

  关键发现

  position_ids 和 attention_mask 的处理位置：

  - 输入时: super().generate() 接收到的确实是 None
  - 处理: 在 LlamaForCausalLM.prepare_inputs_for_generation() 中自动生成
  - 传入 forward: 最终传给 forward 方法时已经是正确的张量

  关键代码在 modeling_llama.py 中:
  if attention_mask is not None and position_ids is None:
      # create position_ids on the fly for batch generation  
      position_ids = attention_mask.long().cumsum(-1) - 1
      position_ids.masked_fill_(attention_mask == 0, 1)

  这就解释了为什么你在 generate 中传入的是 None，但在 forward 中收到的却是有效的张量！

● 总结一下：generate 方法不在 LlamaForCausalLM 中，而是继承自 GenerationMixin，并且在生成过程中会自动处理 position_ids 和 attention_mask 参数，这就是为什么它们在你的 forward 方法中不再是 None 的原因。





两个关键可能错误点：
position_ids 
attention_mask 


假设 position_ids 为 none, 
attention_mask 为 None
从哪里入手。  从最基本的实现 入手，观察其是如何解决 position_ids 和 attention_mask 的 




13:40 - 18:00 务必解决 llava_cacheblend 的问题
- 重写 CacheBlendLlamaModel done
    -- CacheBlendLlamaModel 为什么不直接继承 LlamaModel 
    -- CacheBlendLlamaModel 该类中 应对 position_ids、attention_mask、past_key_values 均完成处理
    -- 除了 关键的 稀疏化 attention 操作 (Qwen25vl 中应该是把 position_embeddings 只初始化一次，而后在decoder layer各层之间传递使用;  hidden_states.shape[1] == len(imp_indices) 判断条件有些苛刻)

- 重写 CacheBlendLlamaDecoderLayer
    -- 目前实现 与 Qwen25vl 中 的 DecoderLayer 完全一致

- 重写 CacheBlendLlamaAttention
    -- CacheBlendAttentionMixin 直接复制 Qwen25vl 中的代码



两个问题：

1. 关于 position_ids，采用的是 和Qwen25vl相同的 位置编码的方式

2. 关于 attention_mask，




关于 llava 的 position_ids 
- 



19:00 - 21:00 解决 llava 关于position_ids 的问题

21:00 - 22:00 测试

22:00 - 23:00 运动




明早：
直接 更改 llava 的 position_ids 问题
优化 android_control 的 prompt指令




问题一：llava模型在 segementation_cache 切割下的 position_ids 问题



问题二：llava模型在 segementation_cache 切割下的 android_control 数据集accuracy
- 可能性一：


- 可能性二：
验证后把现有代码提交 github，然后再研究 position_ids 问题



今日任务：
1. 解决上述基础模型问题
2. 在 pope、android_control 数据集上运行结果
3. 优化 Motivation部分表达 1.5h  done
4. 扩展到 finegym 数据集

5. 迁移 项目至 beiyou(不必须)
    - git项目
    - 模型()
    - 

-- 先在 pope、finegym 数据集上评估完成，再转向 android_control 数据集上


- finetune 模式更新

- segemention read-load 模式

- 更改为 支持 pope数据集



下午任务：
- 扩展到 finegym 数据集 (数据集复制过来)
- llava_segemention 上 position_ids 问题
    -- native forward 逻辑
        position_ids.shape torch.Size([1, 638])
    -- segemention forward 逻辑

晚上任务：
- llava_cacheblend 问题fix
- 绘图


所有的位置编码都是由 position_ids 决定的



